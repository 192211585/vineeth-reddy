#include<stdio.h>
int main()
{
 int i,number,sum=0;
 printf("enter any number:");
 scanf("%d",&number);
 for(i=1;i<number;i++)
 {
  if(number %i==0)
  sum=sum+i;
 }
 if(sum==number)
 printf("%d is a perfect number",number);
 else
  printf("%d is not the perfect number",number);
  return 0;
}
